doveadm_api_key = %{env:MDA_DOVEADM_KEY}
doveadm_allowed_commands =
service doveadm {
   unix_listener doveadm-server {
      user = $SET:default_internal_user
   }
   inet_listener http {
        port = %{env:MDA_DOVEADM_PORT}
        ssl = %{env:MDA_DOVEADM_TLS}
    }
}