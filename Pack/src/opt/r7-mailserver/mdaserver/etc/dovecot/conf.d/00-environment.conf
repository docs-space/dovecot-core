environment {
  MDA_DB_HOST = %{env:MDA_DB_HOST | default('{{ MDA_DB_HOST }}')}
  MDA_DB_PORT = %{env:MDA_DB_PORT | default({{ MDA_DB_PORT }})}
  MDA_DB_NAME = %{env:MDA_DB_NAME | default('{{ MDA_DB_NAME }}')}
  MDA_DB_USER = %{env:MDA_DB_USER | default('{{ MDA_DB_USER }}')}
  MDA_DB_PWD = %{env:MDA_DB_PWD | default('{{ MDA_DB_PWD }}')}

  MDA_DH_KEY = %{env:MDA_DH_KEY | default('{{ MDA_DH_KEY }}')}
  MDA_SSL_CERT = %{env:MDA_SSL_CERT | default('{{ MDA_SSL_CERT }}')}
  MDA_SSL_KEY = %{env:MDA_SSL_KEY | default('{{ MDA_SSL_KEY }}')}

  MDA_LMTP_LISTEN = %{env:MDA_LMTP_LISTEN | default('{{ MDA_LMTP_LISTEN }}')}
  MDA_LMTP_PORT = %{env:MDA_LMTP_PORT | default({{ MDA_LMTP_PORT }})}
  MDA_LMTP_TLS = %{env:MDA_LMTP_TLS | default('{{ MDA_LMTP_TLS }}')}

  MDA_IMAP_LISTEN = %{env:MDA_IMAP_LISTEN | default('{{ MDA_IMAP_LISTEN }}')}
  MDA_IMAP_PORT = %{env:MDA_IMAP_PORT | default({{ MDA_IMAP_PORT }})}

  MDA_IMAPS_LISTEN = %{env:MDA_IMAPS_LISTEN | default('{{ MDA_IMAPS_LISTEN }}')}
  MDA_IMAPS_PORT = %{env:MDA_IMAPS_PORT | default({{ MDA_IMAPS_PORT }})}
  MDA_IMAPS_TLS = %{env:MDA_IMAPS_TLS | default('{{ MDA_IMAPS_TLS }}')}
  
  MDA_HOSTNAME = %{env:MDA_HOSTNAME | default('{{ MDA_HOSTNAME }}')}
  DOVECOT_HOSTNAME = %{env:MDA_HOSTNAME | default('{{ MDA_HOSTNAME }}')}

  MDA_AUTH_LISTEN = %{env:MDA_AUTH_LISTEN | default('{{ MDA_AUTH_LISTEN }}')}
  MDA_AUTH_PORT = %{env:MDA_AUTH_PORT | default({{ MDA_AUTH_PORT }})}
  MDA_AUTH_TLS = %{env:MDA_AUTH_TLS | default('{{ MDA_AUTH_TLS }}')}
  MDA_AUTH_TRUST_NET = %{env:MDA_AUTH_TRUST_NET | default('{{ MDA_AUTH_TRUST_NET }}')}

  MDA_INSTANCE = %{env:MDA_INSTANCE | default('{{ MDA_INSTANCE }}')}
  MDA_DOVEADM_KEY = %{env:MDA_DOVEADM_KEY | default('{{ MDA_DOVEADM_KEY }}')}
  MDA_DOVEADM_PORT = %{env:MDA_DOVEADM_PORT | default({{ MDA_DOVEADM_PORT }})}
  MDA_DOVEADM_TLS = %{env:MDA_DOVEADM_TLS | default('{{ MDA_DOVEADM_TLS }}')}
}

default_internal_user = dovecot
default_internal_group = dovecot
default_login_user = dovenull
instance_name = %{env:MDA_INSTANCE}
hostname = %{env:MDA_HOSTNAME}